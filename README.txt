# InVoice App

-Requirements

-Installations

    -Creating Docker Machines(Otional)
    -Docker Machines create InVoice Container
    -Docker version 19.03.0
    -docker-compose version 1.22.0
    -Create container (''docker-compose up -d)
    -Move to /var/www/html ('docker exec -it invoiceapp_php_web_1 bash')
    -create image ('docker-compose up -d --build')




